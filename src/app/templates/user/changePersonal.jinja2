{% extends '__base.jinja2' %}

{% block content %}
    <main>
    <div>
    <h1>Změna údajů</h1>
        <a href="{{ url_for('user.view_change_password_page') }}">
                <button class="primary">Změnit heslo</button>
            </a>
        <a href="{{ url_for('user.view_change_address_page') }}">
                <button class="primary">Změnit primární adresu</button>
            </a>
        <a href="{{ url_for('user.view_change_secondary_address_page') }}">
                <button class="primary">Změnit sekundární adresu</button>
            </a>

    <form action="#" method="POST">
        {{ form.hidden_tag() }}

        <p>Základní informace</p><br>
        {{ form.first_name.label }}
        {{ form.first_name }}
        <br>

        {{ form.last_name.label }}
        {{ form.last_name }}
        <br>

        {{ form.telephone_number.label }}
        {{ form.telephone_number }}
        <br>

        {{ form.submit() }}
    </form>
        </div>
    </main>
{% endblock %} {# End of block content #}